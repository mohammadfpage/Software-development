# 🧭 راه‌اندازی محیط توسعه (Development Environment Setup)

## 1️⃣ مقدمه

در فرآیند توسعه نرم‌افزار، اولین گام قبل از نوشتن هر خط کد، **راه‌اندازی محیط توسعه (Development Environment)** است.  
محیط توسعه شامل مجموعه‌ای از ابزارها، نرم‌افزارها و تنظیماتی است که به برنامه‌نویس اجازه می‌دهد به‌صورت کارآمد، قابل تکرار و استاندارد روی پروژه کار کند.

هدف از راه‌اندازی محیط توسعه، ایجاد بستری است که:

- تیم بتواند **کد را بنویسد، تست کند و اجرا کند**.  
- وابستگی‌های پروژه (Dependencies) به درستی نصب و مدیریت شوند.  
- از اختلاف در نسخه‌ها و تنظیمات بین سیستم‌های مختلف (مثلاً بین اعضای تیم) جلوگیری شود.  
- پروژه در نهایت قابل **Build و Deploy** روی هر سیستم باشد.

---

## 2️⃣ مفهوم IDE و محیط‌های توسعه

در محیط‌های توسعه امروزی، ابزارهایی مثل **Visual Studio**، **Visual Studio Code**، **IntelliJ IDEA** یا **PyCharm** از جمله IDEها (محیط‌های یکپارچه توسعه) محسوب می‌شوند.

این ابزارها امکاناتی مانند:

- هایلایت و تکمیل خودکار کد  
- دیباگ کردن برنامه  
- مدیریت پکیج‌ها و کتابخانه‌ها  
- کنترل نسخه (Git Integration)  

را در اختیار توسعه‌دهنده قرار می‌دهند.

---

## 3️⃣ هدف از راه‌اندازی محیط توسعه

هدف نهایی از راه‌اندازی محیط توسعه، فراهم کردن بستری پایدار و هماهنگ بین اعضای تیم است تا:

- همه اعضا با تنظیمات یکسان روی پروژه کار کنند؛  
- خطاهای ناشی از تفاوت نسخه‌ها و ابزارها کاهش یابد؛  
- فرآیند **Build، تست، و استقرار (Deployment)** به‌صورت قابل تکرار انجام شود.

---
---

# ⚙️ نصب و آماده‌سازی محیط توسعه

## 2️⃣ نصب محیط‌های یکپارچه توسعه (IDE)

برای شروع کار در هر پروژه نرم‌افزاری، لازم است یک **محیط توسعه یکپارچه (IDE)** یا **ویرایشگر کد (Code Editor)** نصب و تنظیم شود.  
دو ابزار محبوب و پرکاربرد در این زمینه عبارت‌اند از:

---

### 🧩 Visual Studio (ویژوال استودیو)

**Visual Studio** یک محیط توسعه‌ی کامل (IDE) است که برای پروژه‌های بزرگ، مخصوصاً در پلتفرم **.NET** و زبان‌هایی مانند **C#**، **C++** و **Blazor** طراحی شده است.  
این ابزار تمام امکانات لازم برای طراحی، توسعه، تست و انتشار نرم‌افزار را در اختیار برنامه‌نویس قرار می‌دهد.

#### 🔹 مراحل نصب (در ویندوز)
1. دانلود از سایت رسمی [Microsoft Visual Studio](https://visualstudio.microsoft.com/).  
2. در هنگام نصب، انتخاب نوع پروژه (Workload) بر اساس نیاز:
   - `.NET Desktop Development`
   - `ASP.NET and Web Development`
   - `Azure Development`
3. پس از نصب، ورود با حساب Microsoft برای فعال‌سازی و همگام‌سازی تنظیمات.

#### 🔹 ویژگی‌ها
- پشتیبانی داخلی از **Git** برای مدیریت سورس‌کد  
- ابزارهای طراحی رابط کاربری (WinForms, WPF, Razor Pages)  
- پشتیبانی از **IntelliSense** (تکمیل خودکار هوشمند)  
- مدیریت افزونه‌ها از طریق **Visual Studio Marketplace**

#### 🔹 نکته مهم
در محیط‌هایی که دسترسی اینترنت محدود است (مثلاً شبکه‌های آموزشی یا سازمانی)، می‌توان **Workloadها را به‌صورت آفلاین** دانلود و نصب کرد.

---

### 🧩 Visual Studio Code (وی‌اس‌کد)

**VS Code** یک ویرایشگر سبک و چندسکویی (Cross-platform) است که روی ویندوز، لینوکس و macOS اجرا می‌شود.  
این ابزار به‌صورت پیش‌فرض ساده است، اما با نصب **افزونه‌ها (Extensions)** می‌توان آن را برای زبان‌ها و فریم‌ورک‌های مختلف گسترش داد.

#### 🔹 نصب در ویندوز
1. دانلود از سایت رسمی [code.visualstudio.com](https://code.visualstudio.com)  
2. نصب افزونه‌های ضروری:
   - **C#** (برای پروژه‌های .NET)
   - **ESLint** و **Prettier** (برای JavaScript)
   - **Docker** (برای کار با کانتینرها)
   - **GitLens** (برای مشاهده تاریخچه و تغییرات Git)

#### 🔹 نصب در لینوکس (Debian / Ubuntu)
```bash
sudo apt update
sudo apt install code
```

#### 🔹 برای توزیع‌های Fedora / RedHat:
```bash
sudo dnf install code
```




---

# 📦 مدیریت پکیج‌ها در Windows و Linux

بعد از نصب محیط توسعه، قدم بعدی **نصب و مدیریت پکیج‌ها و کتابخانه‌های مورد نیاز پروژه** است.  
پکیج‌ها کدهای آماده‌ای هستند که به پروژه اضافه می‌شوند تا قابلیت‌های خاصی را فراهم کنند، مانند کار با دیتابیس، ساخت API یا رابط کاربری.

در ویندوز و لینوکس، روش‌ها و ابزارها کمی متفاوت است، اما هدف یکسان است: **ایجاد محیط توسعه پایدار و قابل تکرار**.

نکته : با هر تکنولوژی که میخواهید یک برنامه را توسعه بدید نیاز به محیط توسعه دارید ولی خود هر محیط توسعه ای نیاز به پکیج هایی دارد.


---
---

## 1️⃣ ابزارهای مدیریت پکیج

### 🟢 Node.js — npm
- برای پروژه‌های JavaScript و Node.js استفاده می‌شود.
- نصب از وب: می‌توانید آخرین نسخه Node.js و npm را از سایت رسمی دانلود کنید: [https://nodejs.org](https://nodejs.org) و نصب‌کننده را اجرا کنید.
- بعد از نصب، می‌توانید پکیج‌ها را از طریق کامند نصب کنید.

**ویندوز (PowerShell / CMD):**
npm install express  
npm install -g typescript

**لینوکس (bash):**
npm install express  
sudo npm install -g typescript

> نکته: پس از نصب Node.js، npm به صورت خودکار نصب می‌شود و امکان استفاده از سایت یا دستور وجود دارد.

---

### 🟢 Python — pip
- برای نصب کتابخانه‌های Python استفاده می‌شود.
- نصب از وب: آخرین نسخه Python را از سایت رسمی دانلود کنید: [https://www.python.org/downloads/](https://www.python.org/downloads/)
- هنگام نصب روی ویندوز گزینه **Add Python to PATH** را فعال کنید تا pip از هر مسیر قابل استفاده باشد.
- بعد از نصب Python، pip به صورت خودکار نصب می‌شود.

**ایجاد محیط مجازی:**
python -m venv venv

**فعال‌سازی محیط مجازی:**
venv\Scripts\activate      # Windows  
source venv/bin/activate   # Linux / macOS

**نصب پکیج‌ها:**
pip install django  
pip install requests

> نکته: می‌توان برخی پکیج‌ها را مستقیماً از وب سایت PyPI [https://pypi.org/](https://pypi.org/) دانلود و نصب کرد، اما معمولاً pip راحت‌تر و سریع‌تر است.

---

### 🟢 .NET — NuGet / dotnet CLI
- برای پروژه‌های C# و .NET استفاده می‌شود.
- نصب از وب: می‌توانید پکیج‌ها را از NuGet Gallery [https://www.nuget.org/](https://www.nuget.org/) جستجو و دانلود کنید.  
- نصب گرافیکی: در Visual Studio از منوی **Manage NuGet Packages** می‌توانید پکیج را جستجو و نصب کنید.  
- نصب از کامندلاین:  

dotnet add package Newtonsoft.Json  
dotnet restore

---

### 🟢 پکیج‌های سیستمی

#### Linux:
**Debian / Ubuntu:**  
sudo apt install git nodejs python3-pip  

**RedHat / Fedora:**  
sudo yum install git nodejs python3-pip  

#### Windows:
**Chocolatey:**  
choco install git nodejs python  

**Winget:**  
winget install Git.Git  
winget install Python.Python.3

> نکته: برای نصب سیستم‌عاملی معمولاً هم از سایت رسمی نرم‌افزار می‌توان دانلود کرد، هم از Package Manager استفاده نمود.

---

## 2️⃣ نکات مهم و تفاوت‌ها

- مسیر نصب و دسترسی به پکیج‌ها ممکن است در سیستم‌ها متفاوت باشد (`PATH` و مجوزهای Admin).  
- برای نصب پکیج‌های سراسری در ویندوز و لینوکس معمولاً نیاز به **دسترسی مدیر (Admin / sudo)** است.  
- برخی پکیج‌ها می‌توانند مستقیماً از سایت دانلود و نصب شوند، اما روش کامندلاین سریع‌تر، قابل اتومات و تکرارپذیر است.  

---


---

# 🏛️ ریجستری‌های عمومی و خصوصی

بعد از اینکه با مدیریت پکیج‌ها آشنا شدیم، قدم بعدی این است که بدانیم **پکیج‌ها از کجا دانلود می‌شوند**.  
محل دانلود و نگهداری پکیج‌ها به آنچه در اصطلاح **Registry** یا «مخزن» گفته می‌شود، بستگی دارد.

---

## 1️⃣ ریجستری عمومی (Public Registries)

ریجستری عمومی، مکانی است که همه می‌توانند پکیج‌ها را دانلود کنند و معمولا رایگان است.  
مثال‌ها:
```bash
- Node.js → npmjs.org  
- Python → PyPI (Python Package Index)  
- .NET → NuGet.org  
- Java → Maven Central
```
> 🔹 کاربرد: استفاده از کتابخانه‌ها و ابزارهای آماده که عمومی هستند و نیاز به دسترسی خاص ندارند.

---

## 2️⃣ ریجستری خصوصی (Private Registries)

ریجستری خصوصی برای پروژه‌ها یا شرکت‌هایی استفاده می‌شود که می‌خواهند پکیج‌های اختصاصی خود را **فقط بین اعضای تیم یا سازمان خود** به اشتراک بگذارند.

نمونه‌ها:
```bash

- GitHub Packages  
- Azure Artifacts  
- JFrog Artifactory  
- Sonatype Nexus

```
### 🔹 موارد استفاده:
- نگهداری پکیج داخلی تیم یا شرکت  
- کنترل نسخه‌ها و مجوزها (Permissions)  
- محافظت از کد اختصاصی و جلوگیری از دسترسی عمومی

### 🔹 نکته فنی:
برای استفاده از ریجستری خصوصی، معمولاً نیاز به **احراز هویت با token یا credential** داریم.  
مثال برای npm:
```bash
@mycompany:registry=https://npm.pkg.github.com/
```
```bash
//npm.pkg.github.com/:_authToken=ghp_xxxxx
```

> این کار تضمین می‌کند که فقط اعضای تیم بتوانند پکیج‌های خصوصی را دانلود و نصب کنند.

---

---

# 👥 تمرین عملی تیمی — ایجاد پروژه نمونه

هدف این بخش، تمرین عملی برای آشنایی با محیط توسعه، مدیریت پکیج و همکاری تیمی است.

---

## 1️⃣ ایجاد پروژه نمونه

### Visual Studio:
1. باز کردن Visual Studio  
2. انتخاب نوع پروژه (مثلاً Console App یا ASP.NET Web Application)  
3. تعیین مسیر ذخیره پروژه و نام پروژه  
4. انتخاب **Workload** مناسب (.NET Desktop یا ASP.NET)

### VS Code:
1. باز کردن VS Code  
2. ایجاد یک پوشه جدید برای پروژه  
- dotnet new console  
- npm init -y  
- python -m venv venv  

---

## 2️⃣ نصب پکیج‌ها و محیط پایدار

- نصب پکیج‌ها بر اساس نیاز پروژه: برای مثال نصب پکیج های پایتون در ویژوال استدیو


---

## 3️⃣ مدیریت کد و همکاری تیمی

- راه‌اندازی مخزن Git در پروژه:
- git init  
- git add .  
- git commit -m "Initial commit"  

- اشتراک‌گذاری پروژه با اعضای تیم:
- استفاده از GitHub 
- هماهنگی با team branch و merge requests

---




---

# 🌐 آشنایی با محیط‌های Development، Staging و Production

در توسعه نرم‌افزار، معمولاً از چند محیط جداگانه استفاده می‌کنیم تا **امنیت، کیفیت و پایدار بودن پروژه** تضمین شود.  
سه محیط اصلی عبارت‌اند از:

---

## 1️⃣ Development (توسعه)

- محیطی که برنامه‌نویس روی آن **کد می‌زند و آزمایش می‌کند**.  
- معمولاً شامل ابزارهای Debug و logging است.  
- ممکن است برخی پیکربندی‌ها برای راحتی توسعه تغییر داده شوند (مثل اتصال به دیتابیس محلی).  
- هدف: توسعه و تست اولیه بدون تاثیر روی کاربران واقعی.

---

## 2️⃣ Staging (آزمایشی / شبه‌تولید)

- محیطی **مشابه Production**، ولی برای تست نهایی قبل از انتشار استفاده می‌شود.  
- همه داده‌ها و تنظیمات نزدیک به محیط واقعی هستند، اما کاربران واقعی به آن دسترسی ندارند.  
- هدف: شبیه‌سازی رفتار واقعی سیستم و پیدا کردن خطاهای احتمالی قبل از انتشار.

---

## 3️⃣ Production (تولید / واقعی)

- محیطی که کاربران نهایی از آن استفاده می‌کنند.  
- باید پایدار، امن و بدون خطا باشد.  
- معمولاً محدودیت‌های بیشتری برای تغییر مستقیم کد دارد.  
- هدف: ارائه خدمات واقعی و بدون اختلال به کاربران.

---

## 🔹 نکات کلیدی

- هر محیط **تنظیمات و دیتابیس جداگانه** دارد تا تداخلی بین توسعه و استفاده واقعی پیش نیاید.  
- برای تیم‌های بزرگ، رعایت این تفکیک **ضروری و استاندارد صنعت** است.  
- Deployment باید از Development → Staging → Production انجام شود تا ریسک خطا کاهش یابد.

---

